export class FalaOla {
    quantidadeDeOlas: number
    constructor (nome: string)
    dizerOla(nome: string): void
    calcularIdade(anoNascimento: number): number
    dizerTchau(nome: string): void
} 